# Changesets

Runroot uses Changesets for release metadata once package publishing begins.

Phase 1 only establishes the configuration. Release workflows and published versioning policy will be tightened in later phases.
