# Replay Model

Replay is based on immutable events rather than mutable snapshots alone.

Planned model:

- event log as source of truth
- checkpoints for resumability
- derived timeline views for operators

Phase 1 establishes the document set and package layout. Replay implementation is deferred to Phase 4.
