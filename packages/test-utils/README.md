# @runroot/test-utils

Helpers for validating boundary metadata and other shared test fixtures.

Current phase responsibilities:

- provide helpers for workspace smoke tests
- avoid application logic

This package is intentionally small until runtime behavior exists.
