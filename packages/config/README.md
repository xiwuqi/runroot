# @runroot/config

Shared project metadata and boundary types.

Current phase responsibilities:

- define the shared boundary descriptor types
- expose project-level metadata used by docs and operator surfaces
- expose the required quality commands for contributors

This package does not own runtime execution, persistence, or transport logic.
